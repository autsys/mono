{
  "extends": "../../tsconfig.json",
  "exclude": ["**/*.spec.ts"],
  "include": [
    "firestore/**/*.ts",
    "functions/**/*.ts",
    "storage/**/*.ts",
    "auth/**/*.ts",
    "next/**/*.ts"
  ]
}
